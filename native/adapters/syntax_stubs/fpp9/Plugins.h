#pragma once

// Stub. See ../README.md. FPP 9's plugin.cpp includes this but never
// references FPPPlugins:: directly (it registers routes through
// httpserver::webserver instead), so nothing needs to be declared here.
